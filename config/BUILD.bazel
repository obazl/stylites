# load("@bazel_skylib//rules:common_settings.bzl", "string_flag")

label_flag(name = "grammar",
           build_setting_default = "//examples/imp:imp_dso",
            # values = []
            visibility = ["//visibility:public"]
            )
